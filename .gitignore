__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.runtime/
runtime/
debug/
tmp/
output/

# Supplier IMDS reports and anything exported from them: real customer data,
# and inputs rather than source.
*.pdf
*.xlsx
!camds_imds_importer/tests/**/*.pdf
# Scratch pytest run directories
.pytest-*/

# PyInstaller output and its build environment
dist/
build/
.venv-build/
*.spec

# Written by build.ps1 so a running build can name its commit
camds_imds_importer/build_stamp.txt
